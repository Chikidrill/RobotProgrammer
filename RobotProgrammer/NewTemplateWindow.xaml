<Window x:Class="RobotProgrammer.View.NewTemplateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Создать шаблон" Height="400" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,10">
            <TextBlock Text="Название шаблона:"/>
            <TextBox x:Name="TemplateNameBox"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,0,0,10">
            <TextBlock Text="Код шаблона (используйте {ParamName} для параметров):"/>
            <TextBox x:Name="CodeBox" Height="150" TextWrapping="Wrap" AcceptsReturn="True"/>

            <TextBlock Text="Параметры (имя и значение по умолчанию):" Margin="0,10,0,0"/>
            <DataGrid x:Name="ParamsGrid" AutoGenerateColumns="False" CanUserAddRows="True" Height="150">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Имя" Binding="{Binding Key}" Width="*"/>
                    <DataGridTextColumn Header="Значение" Binding="{Binding Value}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" Width="100" Margin="0,0,10,0" Click="Save_Click"/>
            <Button Content="Отмена" Width="100" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
